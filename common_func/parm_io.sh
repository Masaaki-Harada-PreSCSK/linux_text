echo "@@@@@ parm_io @@@@@"
echo "引数1は $1"
echo "引数2は $2"
#
#if [[ $S1 =~ ^[+-]?(?:[0-9]+\.?[0-9]*|\.[0-9]+)([eE][+-]?[0-9]+)?$ ]]; then
#if [[ $1 =~ ^[+-]?(?:\d+\.?\d*|\.\d+)(?:[eE][+-]?\d+)?$ ]]; then
#if [[ $1 =~ ^[+-]?(?:[0-9]+\.?[0-9]*|\.[0-9]+)([eE][+-]?[0-9]+)?$ ]]; then
if [[ "$1" =~ ^-?[0-9]+$ ]]; then
  echo "$1 は数字です"
else
  echo "$1 は数字ではありません"
fi

#if [[ $S2 =~ ^[+-]?(?:[0-9]+\.?[0-9]*|\.[0-9]+)([eE][+-]?[0-9]+)?$ ]]; then
#if [[ $2 =~ ^[+-]?(?:\d+\.?\d*|\.\d+)(?:[eE][+-]?\d+)?$ ]]; then
#if [[ $2 =~ if [[ $2 =~ ^[+-]?(?:[0-9]+\.?[0-9]*|\.[0-9]+)([eE][+-]?[0-9]+)?$ ]]; then ]]; then
#if [[ $2 =~ ^[+-]?(?:[0-9]+\.?[0-9]*|\.[0-9]+)([eE][+-]?[0-9]+)?$ ]]; then
if [[ "$2" =~ ^-?[0-9]+$ ]]; then
  echo "$2 は数字です"
else
  echo "$2 は数字ではありません"
fi

